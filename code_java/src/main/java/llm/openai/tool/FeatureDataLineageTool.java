package llm.openai.tool;

import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;

@RequiredArgsConstructor
@Slf4j
public class FeatureDataLineageTool implements ToolInterface {


  @Tool(description = "查询特征名称对应的数据血缘信息")
  public String featureDataLineage(@ToolParam(name = "featureName", description = "特征名称") String featureName) {
    log.info("featureName: {}", featureName);
    return """
        ### 芝麻分
        
        #### 入参说明
        入参是加工“芝麻分”所需的原始输入数据，通常包括以下参数：
        1. **用户ID**：唯一标识用户的编号，用于关联用户数据。
        2. **年龄**：用户年龄（单位：岁），反映用户生命周期阶段。
        3. **收入水平**：用户月收入或年收入（单位：元），用于评估经济稳定性。
        4. **信用历史**：用户过往信用记录，如贷款还款情况、信用卡使用历史（以天数或次数表示）。
        5. **消费行为**：近期消费频率、金额及类别（如线上购物、线下支付），数据可来自交易记录。
        6. **社交关系**：用户社交网络强度（如联系人数量、互动频率），用于评估社会稳定性。
        7. **数据时间戳**：入参数据的采集时间，确保数据时效性。
        
        **参数说明**：
        - 所有入参均为结构化数据，可从数据库、API或日志中获取。
        - 数据需经过清洗（如处理缺失值、异常值），以确保加工质量。
        
        #### 出参说明
        出参是加工后生成的“芝麻分”特征及相关指标：
        1. **芝麻分**：核心输出，一个数值分数（例如0-1000分），表示用户信用评分。
        2. **分项评分**：可选输出，如年龄分、收入分、信用历史分等，用于解释芝麻分的构成。
        3. **加工时间**：特征生成的时间戳，用于追踪数据新鲜度。
        
        **参数说明**：
        - 芝麻分是连续数值，分数越高表示信用越好。
        - 分项评分可帮助业务方理解分数来源，支持透明度和可解释性。
        
        #### 加工逻辑描述
        加工逻辑基于入参生成出参，步骤如下：
        1. **数据预处理**：对入参进行清洗和标准化。例如，将收入水平归一化到0-1区间，处理信用历史中的缺失值（用平均值填充）。
        2. **特征计算**：
           - **基础分计算**：基于年龄和收入，使用加权公式（如年龄分 = 年龄 × 0.1，收入分 = 收入 × 0.2）生成基础分数。
           - **信用历史调整**：根据还款记录调整分数，逾期次数扣分，良好记录加分。
           - **行为分析**：消费频率和社交关系通过规则引擎或简单模型（如逻辑回归）转换为分数贡献。
        3. **分数合成**：将各分项分数加权求和，生成芝麻分。权重可基于业务经验或历史数据训练确定。
           - 示例公式：芝麻分 = 基础分 × 0.3 + 信用历史分 × 0.4 + 行为分 × 0.3
        4. **后处理**：对芝麻分进行校准（如截断到0-1000范围），并生成分项评分和时间戳。
        
        #### 参数详细说明
        - **入参参数**：
          - 用户ID：字符串类型，必填，用于数据关联。
          - 年龄：整数类型，范围18-100岁，缺失时用默认值（如30岁）处理。
          - 收入水平：浮点数类型，单位元，缺失时基于行业平均值估算。
          - 信用历史：整数类型，表示良好/不良记录次数，缺失时视为0。
          - 消费行为：结构化数据（如JSON），包含频率和金额，缺失时用历史平均值填充。
          - 社交关系：整数类型，表示联系人数量，缺失时用中位数填充。
          - 数据时间戳：日期时间类型，确保数据时效性。
        - **出参参数**：
          - 芝麻分：浮点数类型，范围0-1000，分数越高信用越好。
          - 分项评分：字典类型，包含各分项分数，用于可解释性。
          - 加工时间：日期时间类型，记录特征生成时间。
        
        #### 总结
        “芝麻分”的加工逻辑通过入参数据的预处理、特征计算和分数合成，生成一个综合信用评分。入参覆盖用户基本信息、信用和行为数据，出参提供核心分数及解释指标。该逻辑强调数据质量和迭代优化，以支持信用评估决策。报告旨在提供清晰的加工框架，便于实施和维护。
        """;
  }
}
